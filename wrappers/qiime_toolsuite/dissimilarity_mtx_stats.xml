<?xml version="1.0" ?>
<tool id="dissimilarity_mtx_stats" name="dissimilarity mtx stats" version="1.7.0">
	<description>Calculate mean, median and standard deviation from a set of distance matrices</description>
	<requirements>
		<requirement type="package">qiime</requirement>
	</requirements>
	<command>uncompress_tgz.py -i $input_dir -o dissimilarity_mtx_stats_input;
dissimilarity_mtx_stats.py -i dissimilarity_mtx_stats_input -o dissimilarity_mtx_stats_output;
compress_path.py -i dissimilarity_mtx_stats_output -o $output_dir
</command>
	<inputs>
		<param label="-i/--input_dir: Path to input directory" name="input_dir" type="data"/>
	</inputs>
	<outputs>
		<data format="tgz" name="output_dir"/>
	</outputs>
	<help>
This script reads in all (dis)similarity matrices from an input directory
(input_dir), then calculates and writes the mean, median, standdard deviation
(stdev) to an output folder.

The input_dir must contain only (dis)similarity matrices, and only those you
wish to perform statistical analyses on.


The outputs are in distance matrix format, where each value is the mean,
median, or stdev of that element in all the input distance matrices.
</help>
</tool>
