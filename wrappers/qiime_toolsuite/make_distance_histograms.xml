<?xml version="1.0" ?>
<tool id="make_distance_histograms" name="make distance histograms" version="1.7.0">
	<description>Make distance histograms</description>
	<requirements>
<requirement type="package" version="1.7.0">qiime</requirement>	</requirements>
	<command>make_distance_histograms.py -d $distance_matrix_file -m $map_fname
#if str($prefs_path) != 'None':
 -p $prefs_path
#end if
 -o make_distance_histograms_output
#if str($background_color) != 'None':
 -k $background_color
#end if

#if $monte_carlo:
 --monte_carlo
#end if

#if $suppress_html_output:
 --suppress_html_output
#end if

#if str($fields):
 -f $fields
#end if

#if $monte_carlo_iters:
 --monte_carlo_iters=$monte_carlo_iters
#end if
;
compress_path.py -i make_distance_histograms_output -o $dir_path
</command>
	<inputs>
		<param label="-d/--distance_matrix_file: Input distance matrix filepath (i.e. the result of beta_diversity.py). WARNING: Only symmetric, hollow distance matrices may be used as input. Asymmetric distance matrices, such as those obtained by the UniFrac Gain metric (i.e. beta_diversity.py -m unifrac_g), should not be used as input" name="distance_matrix_file" optional="False" type="data"/>
		<param label="-m/--map_fname: Input metadata mapping filepath." name="map_fname" optional="False" type="data"/>
		<param label="-p/--prefs_path: Input user-generated preferences filepath. NOTE: This is a file with a dictionary containing preferences for the analysis. This dictionary must have a &quot;Fields&quot; key mapping to a list of desired fields. [default: None]" name="prefs_path" optional="True" type="data"/>
		<param label="-k/--background_color: Background color for use in the plots (black or white) [default: white]" name="background_color" optional="True" type="select">
			<option selected="True" value="None">Selection is Optional</option>
			<option value="black">black</option>
			<option value="white">white</option>
		</param>
		<param label="--monte_carlo: Deprecated: pass --monte_carlo_iters &gt; 0 to enable" name="monte_carlo" selected="False" type="boolean"/>
		<param label="--suppress_html_output: Suppress HTML output. [default: False]" name="suppress_html_output" selected="False" type="boolean"/>
		<param default="None" label="-f/--fields: Comma-separated list of fields to compare, where the list of fields should be in quotes (e.g. &quot;Field1,Field2,Field3&quot;). Note: if this option is passed on the command-line, it will overwrite the fields in prefs file. [default: first field in mapping file is used]" name="fields" optional="True" type="text"/>
		<param default="0" label="--monte_carlo_iters: Number of iterations to perform for Monte Carlo analysis. [default: 0; No monte carlo simulation performed]" name="monte_carlo_iters" optional="True" type="integer"/>
	</inputs>
	<outputs>
		<data format="tgz" name="dir_path"/>
	</outputs>
	<help>
To visualize the distance between samples and/or categories in the metadata
mapping file, the user can generate histograms to represent the distances
between samples. This script generates an HTML file, where the user can compare
the distances between samples based on the different categories associated to
each sample in the metadata mapping file.

Distance histograms provide a way to compare different categories and see which
tend to have larger/smaller distances than others. For example, in a hand
study, you may want to compare the distances between hands to the distances
between individuals (with the file &quot;hand_distances.txt&quot; using the parameter -d
hand_distances.txt). The categories are defined in the metadata mapping file
(specified using the parameter -m hand_map.txt). If you want to look at the
distances between hands and individuals, choose the &quot;Hand&quot; field and
&quot;Individual&quot; field (using the parameter --fields Hand,Individual (notice the
fields are comma-delimited)). For each of these groups of distances a
histogram is made. The output is an HTML file which is created in the
&quot;Distance_Histograms&quot; directory (using the parameter -o Distance_Histograms to
specify output directory) where you can look at all the distance histograms
individually, and compare them between each other.


The result of this script will be a folder containing images and/or an HTML
file (with appropriate javascript files), depending on the user-defined
parameters.
</help>
</tool>
